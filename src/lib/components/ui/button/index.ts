import type { ButtonSize, ButtonVariant } from './button.svelte';
import Button, { buttonVariants } from './button.svelte';

export { Button, buttonVariants, Button as Root };
export type { ButtonSize, ButtonVariant };
